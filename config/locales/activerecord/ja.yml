ja:
  activerecord:
    # モデル名
    models:
      user: ユーザー
      post: 投稿
      comment: コメント
      notification: 通知
    # モデルの属性
    attributes:
      post:
        title: SUKIのタイトル
        body: SUKIの理由
        image: メイン画像（Xシェア、みんなのSUKIに反映されます）
      comment:
        body: コメント
      post_image:
        image: サブ画像
        caption: サブ画像の説明文
    # errors.models: バリデーションエラー
