ja:
  activerecord:
    # モデル名
    models:
      user: ユーザー
      post: 投稿
      comment: コメント
      notification: 通知
    # モデルの属性
    attributes:
      post:
        title: SUKIのタイトル
        body: SUKIの理由
      comment:
        body: コメント
    # errors.models: バリデーションエラー
