ja:
  activerecord:
    # モデル名
    models:
      user: ユーザー
      post: 投稿
    # モデルの属性
    attributes:
      post:
        title: SUKIのタイトル
        body: SUKIの理由！
    # errors.models: バリデーションエラー
    errors:
      models:
